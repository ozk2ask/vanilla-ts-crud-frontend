export function renderRegisterPage(appContainer) {
    const action = id === undefined ? '新規登録' `;
    appContainer.innerHTML = ` : , $, { action }, ページ;
    /h2><p>CRUDのC/U部分のロジックをここに書きます;
    /p>`;
    // ここでフォームやデータを描画します
}
//# sourceMappingURL=Register.js.map